# Convex app

This folder contains:
- `schema.ts` — database schema
- `http.ts` — HTTP endpoints (e.g. Stripe webhooks)
- other functions (queries, mutations, actions)

Run locally:
```bash
bunx convex dev
```
